<!DOCTYPE html>
<html lang="id" data-bs-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Panel - <%= page.charAt(0).toUpperCase() + page.slice(1) %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@400;500;600;700&display=swap" rel="stylesheet">

    <style>
        :root {
            --bs-primary-rgb: 88, 80, 236;
            --bs-primary: #5850ec;
            --bs-primary-darker: #4a42d9;
            --bs-body-bg: #f9fafb;
            --bs-body-color: #374151;
            --bs-border-color: #e5e7eb;
            --card-bg: #ffffff;
            --card-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.07), 0 2px 4px -2px rgb(0 0 0 / 0.07);
        }

        [data-bs-theme="dark"] {
            --bs-body-bg: #111827;
            --bs-body-color: #d1d5db;
            --bs-border-color: #374151;
            --bs-primary: #7c73ff;
            --bs-primary-darker: #6a60ec;
            --card-bg: #1f2937;
            --card-shadow: 0 4px 6px -1px rgb(255 255 255 / 0.05), 0 2px 4px -2px rgb(255 255 255 / 0.05);
        }

        body {
            font-family: 'Be Vietnam Pro', sans-serif;
            background-color: var(--bs-body-bg);
            color: var(--bs-body-color);
            font-size: 0.95rem;
        }

        .main-container {
            display: flex;
            min-height: 100vh;
        }

        .content-wrapper {
            display: flex;
            flex-direction: column;
            flex-grow: 1;
            transition: margin-left .3s;
        }

        .content {
            flex-grow: 1;
            padding: 2rem;
        }

        .card {
            border: 1px solid var(--bs-border-color);
            box-shadow: var(--card-shadow);
            border-radius: 0.75rem;
            background-color: var(--card-bg);
        }

        .btn {
            font-weight: 600;
            border-radius: 0.5rem;
        }

        .btn-primary {
            background-color: var(--bs-primary);
            border-color: var(--bs-primary);
        }

        .btn-primary:hover {
            background-color: var(--bs-primary-darker);
            border-color: var(--bs-primary-darker);
        }

        .nav-pills .nav-link {
            border-radius: 0.5rem;
            color: var(--bs-body-color);
        }

        .nav-pills .nav-link.active, .nav-pills .show>.nav-link {
            background-color: var(--bs-primary);
            color: white;
        }

        .form-control, .form-select {
            font-size: 0.95rem;
            background-color: var(--bs-body-bg);
            border-color: var(--bs-border-color);
        }
        .form-control:focus, .form-select:focus {
             background-color: var(--bs-body-bg);
        }

        @media (max-width: 992px) {
            .content { padding: 1rem; }
            .content-wrapper { margin-left: 0 !important; }
        }
    </style>
</head>
<body>
    <div class="main-container">
        <%- include('sidebar') %>
        <div class="content-wrapper">
            <main class="content">
                <button class="btn btn-light border d-lg-none mb-3" type="button" data-bs-toggle="offcanvas" data-bs-target="#sidebar" aria-controls="sidebar">
                    <i class="bi bi-list"></i>
                </button>